# Minimal requirements for web deployment (using mock data)
# This is a lightweight version without heavy ML dependencies

Flask>=2.0.0
Werkzeug>=2.0.0
gunicorn>=20.1.0

# Optional: Add these if you want to use the actual AI system later
# torch>=1.9.0
# torchvision>=0.10.0
# numpy>=1.21.0
# scikit-learn>=1.0.0
# Pillow>=8.3.0

